# DevOps Project Tasks

This document outlines the tasks completed for the DevOps project.

## Task 1: Initialize Repository
- Initialized Git repo with `git init`.
- Added `app.py`, `.gitignore`, and `README.md`.
- Pushed to GitHub: `https://github.com/your-username/devops-project`.

## Task 2: Create Branches
- Created `main` branch (default).
- Created `dev` branch for development.
- Created feature branches: `feature/add-farewell`, `feature/add-error-handling`.

## Task 3: Implement Features
- Added `farewell` function in `feature/add-farewell`.
- Added error handling in `feature/add-error-handling`.

## Task 4: Use Pull Requests
- Merged `feature/add-farewell` into `dev` via PR.
- Merged `feature/add-error-handling` into `dev` via PR.
- Merged `dev` into `main` via PR.

## Task 5: Add Tags
- Tagged `main` as `v1.0.0` for release.

## Task 6: Documentation
- Updated `README.md` with project details.
- Created this `tasks.md` for task documentation.
